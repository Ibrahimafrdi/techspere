<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="TechSpere">

        <meta-data
            android:name="firebase_app_check_provider_debug"
            android:value="androidx.test.services.storage.runner.AndroidTestRunner" />

        <!-- Add any other necessary meta-data or configuration specific to debug -->
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
